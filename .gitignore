# Secrets (WiFi password — keep local only)
esp32-master/config.h
# Note: website/config.js is committed so Vercel can load Supabase (anon key only)

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Arduino build
**/build/
**/.pio/
